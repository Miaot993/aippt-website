---
// src/pages/index.astro
// 引入 Tailwind 生成的全局样式 (关键！)
import '../styles/global.css';
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>AI PPT Master - VIP Resource</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  </head>
  <body class="bg-[#F5F5F4] text-[#1C1917] font-sans antialiased h-screen overflow-hidden flex flex-col items-center justify-center selection:bg-purple-200">

    <div class="fixed inset-0 -z-10 opacity-40 pointer-events-none">
        <div class="absolute top-0 left-0 w-96 h-96 bg-purple-200 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob"></div>
        <div class="absolute top-0 right-0 w-96 h-96 bg-yellow-200 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob animation-delay-2000"></div>
        <div class="absolute -bottom-8 left-20 w-96 h-96 bg-pink-200 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob animation-delay-4000"></div>
    </div>

    <main class="w-full max-w-5xl px-6 relative z-10">
      
      <div id="lock-screen" class="text-center transition-all duration-700 ease-in-out">
        <div class="inline-block px-4 py-1.5 mb-8 border border-gray-300 rounded-full bg-white/50 backdrop-blur-sm">
          <span class="text-xs font-bold tracking-[0.2em] text-gray-500 uppercase">Invitation Only</span>
        </div>
        
        <h1 class="text-6xl md:text-8xl font-serif font-bold text-gray-900 mb-6 tracking-tight leading-tight">
          Design <br/> <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-pink-600">Intelligence</span>
        </h1>
        
        <p class="text-lg text-gray-500 font-light tracking-wide max-w-lg mx-auto mb-12">
          解锁全网顶尖 AI 演示文稿工作流与独家权益。
        </p>

        <div class="flex flex-col items-center gap-4">
          <input 
            type="password" 
            id="password-input"
            placeholder="Enter Access Key" 
            class="text-center text-2xl tracking-[0.5em] font-serif w-64 border-b-2 border-gray-300 bg-transparent focus:border-black focus:outline-none placeholder:text-sm placeholder:font-sans placeholder:tracking-normal placeholder:text-gray-400 py-2 transition-colors"
          />
          <div id="error-msg" class="text-red-500 text-xs tracking-wider opacity-0 transition-opacity">密钥无效 ACCESS DENIED</div>
        </div>
      </div>

      <div id="content-screen" class="hidden opacity-0 translate-y-10 transition-all duration-1000">
        <header class="flex justify-between items-end mb-16 border-b border-gray-300 pb-4">
          <h2 class="text-4xl font-serif font-bold text-gray-900">Library</h2>
          <button onclick="location.reload()" class="text-xs font-bold tracking-widest uppercase text-gray-400 hover:text-black transition-colors cursor-pointer">Logout</button>
        </header>

        <div class="grid md:grid-cols-2 gap-8">
            <a href="https://www.notion.so/VIP-PPT-AI-Bookmark-AI-2fca2b748e4d80c4bcdce13d036e0796?source=copy_link" target="_blank" class="group relative block h-80 bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-2xl transition-all duration-500 cursor-pointer">
                <div class="absolute inset-0 bg-gray-100 group-hover:scale-105 transition-transform duration-700">
                    <div class="w-full h-full bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center">
                        <span class="font-serif text-9xl text-gray-400 font-bold opacity-30 group-hover:opacity-50 transition-opacity">L</span>
                    </div>
                </div>
                <div class="absolute inset-0 p-8 flex flex-col justify-end">
                    <div class="bg-white/90 backdrop-blur-md p-6 rounded-xl translate-y-4 group-hover:translate-y-0 transition-transform duration-500 shadow-lg">
                        <div class="flex justify-between items-center mb-2">
                            <h3 class="text-2xl font-serif font-bold">Lite Plan</h3>
                            <span class="px-2 py-1 bg-gray-100 text-[10px] font-bold uppercase tracking-wider rounded">Free</span>
                        </div>
                        <p class="text-gray-500 text-sm">基础提示词库与入门模版合集。</p>
                    </div>
                </div>
            </a>

            <a href="https://notion.so/your-pro-link" target="_blank" class="group relative block h-80 bg-black rounded-2xl overflow-hidden shadow-sm hover:shadow-2xl transition-all duration-500 cursor-pointer">
                <div class="absolute inset-0 bg-gray-900 group-hover:scale-105 transition-transform duration-700">
                    <div class="w-full h-full bg-gradient-to-br from-purple-900 to-black flex items-center justify-center">
                        <span class="font-serif text-9xl text-white/20 font-bold">P</span>
                    </div>
                </div>
                <div class="absolute inset-0 p-8 flex flex-col justify-end">
                    <div class="bg-white/10 backdrop-blur-md border border-white/10 p-6 rounded-xl translate-y-4 group-hover:translate-y-0 transition-transform duration-500 shadow-lg">
                        <div class="flex justify-between items-center mb-2">
                            <h3 class="text-2xl font-serif font-bold text-white">Pro Max</h3>
                            <span class="px-2 py-1 bg-purple-600 text-[10px] font-bold uppercase tracking-wider text-white rounded">VIP</span>
                        </div>
                        <p class="text-gray-300 text-sm">全套 Cursor 自动化脚本与商业级交付源文件。</p>
                    </div>
                </div>
            </a>
        </div>
      </div>

    </main>

    <script>
      const passwordInput = document.getElementById('password-input');
      const errorMsg = document.getElementById('error-msg');
      const lockScreen = document.getElementById('lock-screen');
      const contentScreen = document.getElementById('content-screen');

      // 🔑 在这里修改密码
      const SECRET_KEY = '8888';

      if(passwordInput) {
        passwordInput.addEventListener('keyup', (e) => {
            const val = e.target.value;
            if (val === SECRET_KEY) {
                // 密码正确
                passwordInput.blur();
                errorMsg.style.opacity = '0';
                
                // 动画转场
                lockScreen.style.opacity = '0';
                lockScreen.style.transform = 'translateY(-20px)';
                
                setTimeout(() => {
                    lockScreen.style.display = 'none';
                    contentScreen.classList.remove('hidden');
                    // 触发重绘
                    requestAnimationFrame(() => {
                        contentScreen.style.opacity = '1';
                        contentScreen.style.transform = 'translateY(0)';
                    });
                }, 600);
            } else if (val.length >= 4) {
                // 密码错误
                errorMsg.style.opacity = '1';
                passwordInput.classList.add('border-red-500');
                setTimeout(() => passwordInput.classList.remove('border-red-500'), 1000);
            }
        });
      }
    </script>
    
    <style>
      /* 添加背景流动动画 */
      @keyframes blob {
        0% { transform: translate(0px, 0px) scale(1); }
        33% { transform: translate(30px, -50px) scale(1.1); }
        66% { transform: translate(-20px, 20px) scale(0.9); }
        100% { transform: translate(0px, 0px) scale(1); }
      }
      .animate-blob {
        animation: blob 7s infinite;
      }
      .animation-delay-2000 {
        animation-delay: 2s;
      }
      .animation-delay-4000 {
        animation-delay: 4s;
      }
    </style>
  </body>
</html>