---
import { Image } from 'astro:assets';
import { Icon } from '@astrojs/starlight/components';

// 接收 Props
const { title, description, href, image, tag = "Featured" } = Astro.props;
---

<a href={href} class="group flex flex-col bg-white dark:bg-zinc-900 border border-gray-200 dark:border-zinc-800 rounded-lg hover:border-indigo-400 dark:hover:border-indigo-500 transition-all duration-300 no-underline h-full overflow-hidden hover:shadow-xl hover:-translate-y-1">
  
  {/* 🛡️ 防御性编程：只有当 image 存在时，才渲染图片区域 */}
  {image && (
    <div class="h-48 overflow-hidden bg-gray-100 dark:bg-zinc-800 border-b border-gray-100 dark:border-zinc-800/50 relative">
      <Image 
        src={image} 
        alt={title} 
        class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" 
      />
      {/* 遮罩层：让图片在深色模式下稍微暗一点，更护眼 */}
      <div class="absolute inset-0 bg-black/0 dark:bg-black/20 group-hover:bg-black/0 transition-colors"></div>
    </div>
  )}

  <div class="flex flex-col gap-2 p-6 flex-1">
    
    <div class="flex items-center justify-between">
      <span class="px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-indigo-600 bg-indigo-50 dark:bg-indigo-900/30 dark:text-indigo-300 rounded-md border border-indigo-100 dark:border-indigo-800">
        {tag}
      </span>
    </div>

    <div class="flex flex-col gap-2 mt-2">
      <h3 class="text-lg font-bold text-gray-900 dark:text-white group-hover:text-indigo-600 dark:group-hover:text-indigo-400 leading-snug transition-colors">
        {title}
      </h3>
      <p class="text-sm text-gray-500 dark:text-gray-400 line-clamp-2 leading-relaxed">
        {description}
      </p>
    </div>
  </div>

</a>